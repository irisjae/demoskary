<!--<component-todos-check-item--confirmed todo__from={ args .todo__from } if={ args .todo__from () .status === 'STARTED' } />-->
<component-schedule-pending-match-item--applying match__from={ match } if={ match () && match () .status === 'PENDING_APPROVAL' && ! match () .away_team } />
<component-schedule-pending-match-item--considering match__from={ match } if={ match () && match () .status === 'PENDING_APPROVAL' && match () .away_team } />
<component-schedule-pending-match-item--waiting match__from={ match } if={ match () && match () .status === 'VERIFIED' } />
<!--<component-check-todos-item--drawn />
<component-check-todos-item--won />-->

<script>
    var item = args .item__from .thru (dropRepeatsWith, json_equal)
    var match = item
        
    item .thru (map, noop) .thru (tap, self .render)
</script>