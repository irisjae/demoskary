<component-dynamic-load-all items__from={ items }>
	<component-tree-control-item item__from={ _item .item } substitute__to={ substitute } />
</component-dynamic-load-all>


<script>
	var tree = 	args .items__from
					.thru (dropRepeatsWith, json_equal)
	var items = mechanism (function () {
			return 	Object .keys (tree ()) .map (function (name) {
						return 	{
									name: name,
									status: tree () [name]
								};
					});
		}, [tree])
		
	var substitute = 	stream ()
							.thru (tap, function () {
								if (! json_equal (tree () [substitute () .item], substitute () .sub))
									args .items__to (with_ (substitute () .item, substitute () .sub) (tree ()))
							})
</script>